# Gradle
.gradle/
.gradle_user/
build/
app/build/
app/build_old_root/

# IDE
.idea/
*.iml
local.properties

# APK outputs (use GitHub Releases instead)
*.apk

# Signing key (DO NOT commit to public repo)
release.keystore

# Bundled models (too large for git, download before building)
app/src/main/assets/*.onnx
app/src/main/assets/sensevoice/

# OS
.DS_Store
Thumbs.db
